#import "../fragments/entireInputColumn.graphql"

mutation deleteInputColumnAndUpdateAttribute($attributeId: ID!, $inputColumnId: ID!) {
    deleteInputColumnAndUpdateAttribute(attributeId: $attributeId, inputColumnId: $inputColumnId) {
        ...entireInputColumn
    }
}
