#import "./fragmentEntireAttribute.graphql"

mutation deleteInputColumn($attributeId: ID!, $inputColumnId: ID!) {
    updateAttribute(
        id: $attributeId,
        data: {
            inputColumns: {
                delete: [
                    {
                        id: $inputColumnId
                    }
                ]
            }
        }
    ) {
        ...entireAttribute
    }
}
