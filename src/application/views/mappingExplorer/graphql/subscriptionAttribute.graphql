#import "./fragmentEntireAttribute.graphql"

subscription subscriptionAttribute ($id: ID!) {
    attribute(id: $id) {
        updatedFields
        node {
            ...entireAttribute
        }
    }
}
