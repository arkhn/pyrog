#import "../fragments/resourceRecAttributes.graphql"

subscription subscriptionAttribute ($id: ID!) {
    attribute(id: $id) {
        updatedFields
        node {
            ...resourceRecAttributes
        }
    }
}
