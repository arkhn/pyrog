query getResource($database: String!, $resource: String) {
    resources (
        where: {
            name: $resource,
            database: {
                name: $database,
            }
        }
    ) {
        id
        name
        primaryKeyOwner
        primaryKeyTable
        primaryKeyColumn
    }
}
