#import "./fragmentEntireResource.graphql"

query getResource($database: String!, $resource: String!) {
    getResource (
        resource: $resource,
        database: $database,
    ) {
        id
        name
        primaryKeyOwner
        primaryKeyTable
        primaryKeyColumn
    }
}
