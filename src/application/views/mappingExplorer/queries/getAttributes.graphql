query Attributes($database: String!, $resource: String!, $attribute: String!){
  attributes (
    where: {
      name: $attribute
      resource: {
        name: $resource
        database: {
          name: $database
        }
      }
  	}
  ) {
    id
    name
    inputColumns {
        id
        owner
        table
        column
        script
        joinSourceColumn
        joinTargetOwner
        joinTargetTable
        joinTargetColumn
    }
  }
}
