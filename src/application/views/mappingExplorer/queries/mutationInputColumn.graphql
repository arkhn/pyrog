#import "./fragmentEntireInputColumn.graphql"
#import "./fragmentEntireJoin.graphql"

mutation mutationInputColumn($id: ID!, $data: InputColumnUpdateInput) {
    updateInputColumn(id: $id, data: $data) {
        ...entireInputColumn
        joins {
            ...entireJoin
        }
    }
}
