#import "./fragmentEntireResource.graphql"

subscription subscriptionResource($id: ID!) {
    resourceSubscription (id: $id) {
        updatedFields
        node {
            ...entireResource
        }
    }
}
