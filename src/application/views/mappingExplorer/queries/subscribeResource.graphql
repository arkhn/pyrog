subscription subscribeResource($id: ID!) {
    resourceSubscription (id: $id) {
        updatedFields
        node {
            id
            name
            primaryKeyOwner
            primaryKeyTable
            primaryKeyColumn
        }
    }
}
