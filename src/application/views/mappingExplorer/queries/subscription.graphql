subscription subscribeToInputColumn($id: ID!) {
    inputColumnSubscription(id: $id) {
        updatedFields
        node {
            id
            owner
            column
            table
            script
            joinSourceColumn
        }
    }
}