#import "./fragmentEntireInputColumn.graphql"

subscription subscribeToInputColumn($id: ID!) {
    inputColumn(id: $id) {
        updatedFields
        node {
            ...entireInputColumn
        }
    }
}
