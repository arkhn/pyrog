#import "../fragments/entireJoin.graphql"

mutation deleteJoinAndUpdateInputColumn($inputColumnId: ID!, $joinId: ID!) {
    deleteJoinAndUpdateInputColumn(inputColumnId: $inputColumnId, joinId: $joinId) {
        ...entireJoin
    }
}
