query structureDisplay($definitionId: ID!) {
  structureDefinition(definitionId: $definitionId) {
    id
    attributes {
      id
      attribute
      extensions {
        id
        name
        publisher
        attributes {
          attribute
        }
      }
    }
  }
}
