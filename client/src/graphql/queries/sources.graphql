query sources {
  sources {
    id
    name
    hasOwner
    mappingProgress
    template {
      id
      name
    }
    credential {
      id
    }
    accessControls {
      id
      user {
        id
        email
      }
      role
    }
  }
}
