#import "../fragments/entireInputGroup.graphql"
#import "../fragments/comment.graphql"

query inputsForAttribute($attributeId: ID!) {
  attribute(attributeId: $attributeId) {
    id
    inputGroups {
      ...entireInputGroup
    }
    comments {
      ...comment
    }
  }
}
