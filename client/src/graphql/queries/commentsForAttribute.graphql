query commentsForAttribute($attributeId: ID!) {
  attribute(attributeId: $attributeId) {
    id
    comments {
      id
      author {
        id
        name
      }
      content
      createdAt
    }
  }
}
