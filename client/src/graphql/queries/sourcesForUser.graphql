query sourcesForUser($userId: ID!) {
  sourcesForUser(userId: $userId) {
    id
    name
    hasOwner
    mappingProgress
    template {
      id
      name
    }
    credential {
      id
    }
  }
}
