query exportMapping($sourceId: ID!, $includeComments: Boolean) {
  source(sourceId: $sourceId) {
    id
    name
    template {
      id
      name
    }
    mapping(includeComments: $includeComments)
  }
}
