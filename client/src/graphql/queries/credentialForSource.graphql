query credentialForSource($sourceId: ID!) {
  source(sourceId: $sourceId) {
    id
    credential {
      id
      host
      port
      database
      model
      login
      decryptedPassword
      owner
      schema
    }
  }
}
