query resourcesForSource($sourceId: ID!) {
  source(sourceId: $sourceId) {
    id
    resources {
      id
      label
      primaryKeyOwner
      primaryKeyTable
      primaryKeyColumn
      definition {
        id
        type
      }
    }
  }
}