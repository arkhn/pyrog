mutation createComment($attributeId: ID!, $content: String!) {
  createComment(attributeId: $attributeId, content: $content) {
    id
    author {
      id
      name
    }
    content
    createdAt
  }
}
