#import "../fragments/entireInputGroup.graphql"

mutation deleteCondition($inputGroupId: ID!, $conditionId: ID!) {
  deleteCondition(inputGroupId: $inputGroupId, conditionId: $conditionId) {
    ...entireInputGroup
  }
}
