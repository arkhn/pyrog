mutation createSource(
  $templateName: String!
  $name: String!
  $hasOwner: Boolean!
  $mapping: String!
) {
  createSource(
    templateName: $templateName
    name: $name
    hasOwner: $hasOwner
    mapping: $mapping
  ) {
    id
  }
}
