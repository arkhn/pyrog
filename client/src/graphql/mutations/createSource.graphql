mutation createSource(
  $templateName: String!
  $name: String!
  $credentialId: String!
  $mapping: String
) {
  createSource(
    templateName: $templateName
    name: $name
    credentialId: $credentialId
    mapping: $mapping
  ) {
    id
  }
}
