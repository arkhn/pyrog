mutation createSource(
  $templateName: String!,
  $name: String!,
  $hasOwner: Boolean!
) {
  createSource(
    templateName: $templateName,
    name: $name,
    hasOwner: $hasOwner
) {
    id
  }
}
