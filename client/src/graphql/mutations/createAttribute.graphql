#import "../fragments/entireAttribute.graphql"

mutation createAttribute($resourceId: ID!, $path: String!, $data: AttributeInput) {
  createAttribute(resourceId: $resourceId, path: $path, data: $data) {
    ...entireAttribute
  }
}
