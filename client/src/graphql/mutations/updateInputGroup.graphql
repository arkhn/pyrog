#import "../fragments/entireInputGroup.graphql"

mutation updateInputGroup(
  $inputGroupId: ID!
  $mergingScript: String
  $conditionId: String
) {
  updateInputGroup(
    inputGroupId: $inputGroupId
    mergingScript: $mergingScript
    conditionId: $conditionId
  ) {
    ...entireInputGroup
  }
}
