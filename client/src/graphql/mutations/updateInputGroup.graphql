#import "../fragments/entireInputGroup.graphql"

mutation updateInputGroup($inputGroupId: ID!, $data: InputGroupInput!) {
  updateInputGroup(inputGroupId: $inputGroupId, data: $data) {
    ...entireInputGroup
  }
}
