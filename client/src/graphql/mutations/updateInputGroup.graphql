#import "../fragments/entireInputGroup.graphql"

mutation updateInputGroup($inputGroupId: ID!, $mergingScript: String) {
  updateInputGroup(inputGroupId: $inputGroupId, mergingScript: $mergingScript) {
    ...entireInputGroup
  }
}
