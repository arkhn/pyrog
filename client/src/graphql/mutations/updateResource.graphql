#import "../fragments/entireResource.graphql"

mutation updateResource($resourceId: ID!, $data: UpdateResourceInput!, $filters: [FilterInput!]) {
  updateResource(resourceId: $resourceId, data: $data, filters: $filters) {
    ...entireResource
  }
}
