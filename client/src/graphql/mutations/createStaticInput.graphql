#import "../fragments/entireInput.graphql"
#import "../fragments/entireAttribute.graphql"

mutation createStaticInput($attributeId: ID!, $staticValue: String) {
  createInput(attributeId: $attributeId, static: $staticValue) {
    ...entireInput
    attribute {
      ...entireAttribute
    }
  }
}
