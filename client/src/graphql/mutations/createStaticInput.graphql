#import "../fragments/entireInput.graphql"
#import "../fragments/entireAttribute.graphql"

mutation createStaticInput($inputGroupId: ID!, $staticValue: String) {
  createInput(inputGroupId: $inputGroupId, static: $staticValue) {
    ...entireInput
    inputGroup {
      id
      attribute {
        ...entireAttribute
      }
    }
  }
}
