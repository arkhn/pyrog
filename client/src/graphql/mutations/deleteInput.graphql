#import "../fragments/entireInputGroup.graphql"

mutation deleteInput($inputGroupId: ID!, $inputId: ID!) {
  deleteInput(inputGroupId: $inputGroupId, inputId: $inputId) {
    ...entireInputGroup
  }
}
